import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.investdiscover',
  appName: 'invest-discover-page-app',
  webDir: 'www'
};

export default config;
